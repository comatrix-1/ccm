# URL of the Vercel proxy endpoint (injected at build time by Vite)
# Replace with your actual deployed Vercel project URL after deployment.
VITE_PROXY_URL=https://your-project.vercel.app/api/semantic-check

# Git Workflow

After completing each spec task:
1. Stage all changed files with `git add -A`
2. Commit with a conventional commit message describing what was implemented in the task
3. Do not batch multiple tasks into one commit